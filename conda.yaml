name: mm-dino
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24

# Install the CUDA-specific PyTorch build before requirements-segmentation.txt.
# Do not add an unqualified torch/torchvision dependency here: it can resolve to
# a build that does not contain RTX 5090 (Blackwell, sm_120) kernels.
